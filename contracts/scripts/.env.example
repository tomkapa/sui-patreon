# Sui Network Configuration
SUI_NETWORK=testnet  # Options: mainnet, testnet, devnet, localnet

# RPC URL (optional - defaults to Mysten Labs public RPC)
# RPC_URL=https://fullnode.testnet.sui.io:443

# Your wallet private key (required)
# Get this from: sui keytool export --key-identity <your-address>
# Format: 64-character hex string (with or without 0x prefix)
PRIVATE_KEY=your_private_key_here

# Deployed package ID (set after deploying contracts)
# Get this from the deployment output
PACKAGE_ID=
